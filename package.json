{
  "name": "newui5app",
  "version": "0.0.1",
  "description": "Create a UI5 application to manage Employees. App name will be employeeapp\nThe app should have a tab",
  "scripts": {
    "start": "ui5 serve",
    "build": "ui5 build --clean-dest"
  },
  "devDependencies": {
    "@ui5/cli": "^3.0.0"
  }
}